os: linux
sudo: required
env: DISPLAY=':99.0'
dist: trusty
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

language: ruby
rvm:
  - 2.3.0
 
before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - sleep 3 # give xvfb some time to start
  - bundle exec jekyll serve  # start a Web server
  - sleep 3 # give Web server some time to bind to sockets, etc

script:
  - bundle exec rspec
